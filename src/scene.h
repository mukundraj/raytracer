#ifndef SCENE_H
#define SCENE_H

#include <string>
#include <hittable_list.h>
#include <mesh_obj.h>

hittable_list get_teapot_scene(std::string teapot_obj_path){

  hittable_list hlist;

  return hlist;
}

#endif /* SCENE_H */
